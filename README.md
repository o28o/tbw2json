# tbw2json
Migrate tbw data from html to json


```
git clone this repo
download tbw site data and unzip
install python3.X
pip install beautifulsoup4
```

run it with the following command
```
find path/to/bw/dir/nikaya/ -type f | xargs -I {} python3.yourVersion path/to/soup.py {}
```
